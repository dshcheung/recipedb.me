app.controller('RecipeCtrl', ['$scope', '$http', '$routeParams', function($scope, $http, $routeParams){

  // get recipe information to display
  $http.get('/recipes/'+$routeParams.id+'.json').success(function(data, status, xhr){
    $scope.recipe = data;
    console.log(data);
  });
}]);
