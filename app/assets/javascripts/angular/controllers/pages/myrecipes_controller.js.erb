app.controller('MyRecipesCtrl', ['$scope', '$http', '$routeParams', function($scope, $http, $routeParams){

  $http.get('/myrecipes.json').success(function(data, status, xhr){
    $scope.recipes = data.recipes;
  });
  
  $scope.cuisineCollapsed = true;
  $scope.courseCollapsed = true;
}]);