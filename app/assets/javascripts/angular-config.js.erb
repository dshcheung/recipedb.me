var app = angular.module("recipedb",['ngRoute', 'ui.bootstrap']);
  
app.config(['$routeProvider', function($routeProvider){
  $routeProvider.when('/', {
    templateUrl: "<%= asset_path('home.html') %>",
    controller: 'HomeCtrl' // the name of this controller should match with the actual name of the actual controller
  })
  .when('/search', {
    templateUrl: "<%= asset_path('recipeFinder.html') %>",
    controller: 'RecipeFinderCtrl'
  })
  .when('/recipes/:id', {
    templateUrl: "<%= asset_path('recipe.html') %>",
    controller: 'RecipeCtrl'
  })
  .when('/myrecipes/:id', {
    templateUrl: "<%= asset_path('myRecipes.html') %>",
    controller: 'MyRecipeCtrl'
  })


}]);

// $(function () {
//   $('[data-toggle="tooltip"]').tooltip()
// })